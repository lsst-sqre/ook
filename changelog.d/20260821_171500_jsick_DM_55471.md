### Other changes

- Widened the API field-naming guard to cover bare calendar dates (`datetime.date`), not only timestamps (`datetime.datetime`), so a date-valued field that omits the `date_` prefix is caught as well.
